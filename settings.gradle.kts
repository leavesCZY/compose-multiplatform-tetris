pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

rootProject.name = "compose_tetris"
include(":common")
include(":android")
include(":desktop")